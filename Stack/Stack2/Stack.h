#ifndef STACK_H		// STACK_H가 정의되어있지 않다면,		// 헤더파일 내용이 한번만 붙을 수 있도록 함
#define STACK_H

// #pragma once		// 헤더파일이 한번만 들어가도록 함

void push(int);     // 함수 정의 or 선언 있어야함. 선언된 것은 정의가 있어야함..
int pop(void);

#endif